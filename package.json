{
  "name": "semilla-ecommerce",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "node serve.mjs",
    "db:init": "wrangler d1 execute shop --remote --file=./public/data/schema.sql",
    "db:init:local": "wrangler d1 execute shop --local --file=./public/data/schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "stripe": "^18.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
