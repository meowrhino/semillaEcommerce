/**
 * pages/gracias.js — post-pago OK: monta la cabecera y vacía el carrito.
 */
import { mountLayout } from "../core/layout.js";
import { clearCart } from "../core/cart.js";

mountLayout();
clearCart();
